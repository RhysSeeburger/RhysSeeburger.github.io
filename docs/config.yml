# website name
title: Rhys Seeburger
# website URL
baseURL: 'https://rhysseeburger.github.io'

#theme
theme: "default"
# content directory
sourcedir: "content"
# Anything to add to the build into a static folder
staticdir: "static"
# build directory
builddir: "_build/html"

# setup markdown options
markdown:
  # https://python-markdown.github.io/extensions/
  extensions:
    - extra